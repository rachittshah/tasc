# Trusted build bootstrap for the three source-only distributions in
# requirements.lock. Install this hash-locked environment first, then install
# requirements.lock with --no-build-isolation.
setuptools==83.0.0
wheel==0.46.3
