# Direct constraints for regenerating requirements.lock. Follow README.md's
# two-stage hash-locked install; do not install this input file directly.
# Use Python 3.12 on Apple Silicon; MLX does not support non-Apple platforms.
mlx==0.31.1
mlx-metal==0.31.1
mlx-lm[evaluate]==0.31.1
lm-eval==0.4.12
transformers==5.14.1
datasets==5.0.0
